{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
<section class="relative">
    <img src="{{ asset('img/banniere.jpg') }}" alt="Bannière" class="h-[780px] md:h-full w-full object-cover z-0">
    <div class="absolute inset-0 bg-black opacity-50"></div>
    <div class="absolute inset-0 flex flex-col justify-center items-center md:items-start px-6 md:px-24 z-10">
        <h1 class="text-white text-2xl text-center md:text-left md:text-4xl font-semibold mb-4">
            Waw - Une solution pour revivre son Roadtrip !
        </h1>

        <a href="#roadtrip" class="text-white font-semibold text-lg md:text-2xl bg-button hover:bg-blackgreen rounded-lg px-4 py-2 md:px-6 md:py-2 transition duration-300">
            En savoir plus
        </a>
    </div>
</section>

<section class="relative px-6 md:px-24 mt-6">
    <h1 class="text-2xl md:text-4xl md:text-left text-center font-semibold mb-6">A propos</h1>
    <p class="indent-4 md:indent-8 text-base md:text-lg leading-relaxed">
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Dolore sequi aut laborum adipisci totam nemo fuga. 
        Eum ut suscipit illum mollitia officiis eaque accusantium odit? Mollitia, doloribus officiis? Pariatur, maxime!
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Impedit distinctio consequuntur facere, voluptate itaque 
        veniam, nostrum mollitia quas unde labore deleniti. Velit quis earum nesciunt delectus? Quos, facere. Totam, dolore.
    </p>
</section>

<section id="roadtrip" class="relative px-6 md:px-24 mt-6">
    <h1 class="text-2xl md:text-4xl font-semibold mb-6 md:text-left text-center">Liste des road trips créés par la communauté</h1>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 mx-auto">
        {% for voyages in communityVoyage|slice(0, 4) %}
            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <img src="{{ asset('uploads/' ~ voyages.coverImage) }}" alt="{{ voyages.title }}" class="w-full h-40 md:h-64 object-cover rounded-t-lg">
                <div class="p-2 text-center bg-forestgreen">
                    <p class="text-sm md:text-lg font-semibold text-white">De {{ voyages.user.username }}</p>
                </div>
                <div class="w-full p-2 text-center text-white font-semibold bg-blackgreen">
                    <a href="{{ path('app_search_roadtrip', {'id': voyages.id}) }}">En savoir plus &#x3009;</a>
                </div>
            </div>
        {% else %}
            <p class="text-center">Aucun voyage communautaire disponible.</p>
        {% endfor %}
    </div>
    <a class="block mx-auto mt-6 mb-24 text-center w-2/3 sm:w-1/3 p-2 rounded-lg text-white font-semibold bg-button hover:bg-blackgreen" 
        href="{{ path('app_roadtrips') }}">En savoir plus</a>
</section>

{% endblock %}
